BACKEND_API_BASE_URL=http://localhost:8700

# Encrypts/signs the iron-session cookie. Must be >= 32 characters.
# Generate one locally, e.g.: openssl rand -base64 32
SESSION_PASSWORD=replace-this-with-a-random-32-character-minimum-secret
